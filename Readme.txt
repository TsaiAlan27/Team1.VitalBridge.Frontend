最簡 Vite 使用說明

在專案根執行以下 PowerShell 指令：

```powershell
npm install
npm run dev        # 開發（熱重載）
# 若要檢查打包結果：
npm run build
npm run preview
```

備註：若 `package.json` 已包含 `"dev": "vite"`，只要 `npm install` 後執行 `npm run dev` 即可。
